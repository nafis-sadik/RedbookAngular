import { BlumeDropDirective } from './blume-drop.directive';

describe('BlumeDropDirective', () => {
  it('should create an instance', () => {
    const directive = new BlumeDropDirective();
    expect(directive).toBeTruthy();
  });
});
